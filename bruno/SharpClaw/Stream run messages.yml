info:
  name: Stream run messages
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/sessions/{{sessionId}}/runs/{{runId}}/stream"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
