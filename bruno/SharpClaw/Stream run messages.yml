info:
  name: Stream run messages
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/sessions/{{sessionId}}/messages/{{latestSequenceId}}/stream"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
