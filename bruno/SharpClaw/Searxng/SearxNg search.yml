info:
  name: SearxNg search
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/search?q=Terminal.GUI&format=json
  headers:
    - name: Accept
      value: application/json
      disabled: true
  params:
    - name: q
      value: Terminal.GUI
      type: query
    - name: format
      value: json
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
