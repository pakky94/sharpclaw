info:
  name: Get run messages
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/sessions/{{sessionId}}/runs/{{runId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
