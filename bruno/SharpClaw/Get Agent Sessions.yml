info:
  name: Get Agent Sessions
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/agents/{{agentId}}/sessions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
