info:
  name: Debug Tool
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseUrl}}/debugging/tool-call"
  body:
    type: json
    data: |-
      {
        "toolName": "web_search",
        "arguments": {
          "query": "Terminal.GUI"
        }
        //"toolName": "web_fetch",
        //"arguments": {
        //  "url": "https://github.com/gui-cs/Terminal.Gui"
        //}
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        //let data = res.getBody();
        //bru.setEnvVar("sessionId", data.sessionId);
        //bru.setEnvVar("runId", data.runId);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
